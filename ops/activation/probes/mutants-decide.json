[
  { "id": "D1", "note": "torn or inconsistent ledger ignored", "from": "if (fold.integrity !== \"intact\" || fold.inconsistencies.length > 0) {", "to": "if (false) {" },
  { "id": "D2", "note": "an ended attempt keeps acting (round 5 A3)", "from": "if (fold.attemptEnded !== null) {", "to": "if (false) {" },
  { "id": "D3", "note": "abort after the gate tears down the armed run", "from": "teardown: !stepDone(fold, \"10-gate\"), nextOwnerAction", "to": "teardown: true, nextOwnerAction" },
  { "id": "D3b", "note": "abort before the gate leaves the tasks as they are", "from": "teardown: !stepDone(fold, \"10-gate\"), nextOwnerAction", "to": "teardown: false, nextOwnerAction" },
  { "id": "D4", "note": "unknown world readings pass (A1)", "from": "if (world.red.length > 0 || world.unknown.length > 0) {", "to": "if (world.red.length > 0) {" },
  { "id": "D5", "note": "red world findings pass", "from": "if (world.red.length > 0 || world.unknown.length > 0) {", "to": "if (world.unknown.length > 0) {" },
  { "id": "D6", "note": "task definitions never checked by value", "from": "if (stepDone(fold, \"1-install\")) red.push(...definitionFindings(name, task));", "to": "" },
  { "id": "D7", "note": "any session lead-in accepted", "from": "if (name !== \"cycle\" || takeValue() !== SESSION_LEAD_IN_DEFAULT)", "to": "if (name !== \"cycle\")" },
  { "id": "D8", "note": "-SkipOutsideSession:$false accepted", "from": "if (name !== \"cycle\" || inline?.toLowerCase() !== \"$true\")", "to": "if (name !== \"cycle\")" },
  { "id": "D9", "note": "abbreviated or unknown parameters accepted", "from": "      default:", "to": "      default: break;" },
  { "id": "D10", "note": "host options before -File not checked (-Command)", "from": "if (tokens.slice(0, fileAt).join(\" \").toLowerCase() !== \"-noprofile -noninteractive -executionpolicy bypass\")", "to": "if (false)" },
  { "id": "D11", "note": "any script behind -File accepted", "from": "if (script === undefined || !script.toLowerCase().endsWith(expectedScript))", "to": "if (script === undefined)" },
  { "id": "D12", "note": "certificate line tolerated between step 0 and the gate (the latch)", "from": "if (line === \"absent\" && env.certificatePath !== null)", "to": "if (false)" },
  { "id": "D13", "note": "after the gate any certificate path accepted", "from": "env.certificatePath !== recordedString(fold, \"2-certificate\", \"certificatePath\")", "to": "false" },
  { "id": "D14", "note": "profile not checked (ACT-57)", "from": "if (env.profile !== \"competition\")", "to": "if (false)" },
  { "id": "D15", "note": "resolved account not compared", "from": "else if (observations.resolvedAccountMasked.value !== schedule.longRunAccountMasked)", "to": "else if (false)" },
  { "id": "D16", "note": "a wrapper hash not compared (review 2026-09-14, point 6)", "from": "if (observations.wrapperHashes.value[name] !== recorded[name]) red.push(", "to": "if (false) red.push(" },
  { "id": "D17", "note": "digests not re-checked after the certificate (ACT-25)", "from": "else if (observations.deploymentDigests.value.runtimeDigest !== runtimeDigest || observations.deploymentDigests.value.policyDigest !== policyDigest)", "to": "else if (false)" },
  { "id": "D18", "note": "disarm registered for any time", "from": "} else if (!disarm.registered || disarm.fires === null || compareLocal(disarm.fires, localAt(schedule.anchorDay, 15, 5)) !== 0) {", "to": "} else if (!disarm.registered) {" },
  { "id": "D19", "note": "reboot closed ok without a later boot", "from": "const rebooted = observations.bootUtcMs.value > state.intentAtUtcMs;", "to": "const rebooted = true;" },
  { "id": "D20", "note": "reboot close bound by its own window instead of the re-arm's", "from": "const deadline = stepWindow(\"7-rearm\", schedule).notValidAfter;", "to": "const deadline = stepWindow(\"8-reboot\", schedule).notValidAfter;" },
  { "id": "D21", "note": "any interrupted step closed like the reboot", "from": "if (state.step !== \"8-reboot\") {", "to": "if (false) {" },
  { "id": "D22", "note": "a failed step in this attempt is retried", "from": "if (state !== undefined && state.attempt === attempt.id && (state.outcome === \"failed\" || state.outcome === \"unknown\")) {", "to": "if (false) {" },
  { "id": "D22b", "note": "a failure from a previous attempt aborts the retry", "from": "state.attempt === attempt.id && (state.outcome", "to": "(state.outcome" },
  { "id": "D23", "note": "deadlines ignored (round 2 A2)", "from": "if (compareLocal(observations.nowLocal, window.notValidAfter) > 0) {", "to": "if (false) {" },
  { "id": "D24", "note": "steps act before their window opens", "from": "if (window.opens !== null && compareLocal(observations.nowLocal, window.opens) < 0) {", "to": "if (false) {" },
  { "id": "D25", "note": "stale alert confirmation accepted", "from": "if (age < 0 || age > ALERT_CONFIRMATION_MAX_AGE_MS)", "to": "if (age < 0)" },
  { "id": "D25b", "note": "confirmation of exactly fourteen days refused", "from": "age > ALERT_CONFIRMATION_MAX_AGE_MS", "to": "age >= ALERT_CONFIRMATION_MAX_AGE_MS" },
  { "id": "D25c", "note": "future-dated confirmation accepted", "from": "if (age < 0 || age >", "to": "if (age >" },
  { "id": "D26", "note": "rotated checks outlive the confirmation (section 8.13)", "from": "if (confirmation !== null && confirmation.fingerprints[name] !== check.fingerprint)", "to": "if (false)" },
  { "id": "D27", "note": "step 0 accepts a down check", "from": "if (check.status !== \"up\" && check.status !== \"paused\") red.push(", "to": "if (false) red.push(" },
  { "id": "D28", "note": "certificate policy digest not compared", "from": "if (deployed.runtimeDigest !== certificate.digests.runtimeDigest || deployed.policyDigest !== certificate.digests.policyDigest) {", "to": "if (deployed.runtimeDigest !== certificate.digests.runtimeDigest) {" },
  { "id": "D29", "note": "long-run artefacts compared case-sensitively", "from": "const found = observations.longRunArtefacts.value.filter(entry => forbidden.includes(entry.toLowerCase()));", "to": "const found = observations.longRunArtefacts.value.filter(entry => forbidden.includes(entry));" },
  { "id": "D29b", "note": "long-run contamination ignored", "from": "const found = observations.longRunArtefacts.value.filter(entry => forbidden.includes(entry.toLowerCase()));", "to": "const found: string[] = [];" },
  { "id": "D30", "note": "certificate verdict ignored (ACT-13)", "from": "if (certificate.verdict !== \"PASS\") {", "to": "if (false) {" },
  { "id": "D31", "note": "open orders ignored by the flat check (ACT-14)", "from": "if (account.positions !== 0 || account.nonTerminalOrders !== 0) {", "to": "if (account.positions !== 0) {" },
  { "id": "D32", "note": "disarm registered on the certificate day", "from": "{ kind: \"register-disarm\", at: localAt(schedule.anchorDay, 15, 5) }", "to": "{ kind: \"register-disarm\", at: localAt(schedule.certificateDay, 15, 5) }" },
  { "id": "D33", "note": "5a acts without an observed firing", "from": "if (firing === null || watchdog.status !== \"up\" || watchdog.lastPingUtcMs === null || watchdog.lastPingUtcMs <= enabledAt) {", "to": "if (watchdog.status !== \"up\") {" },
  { "id": "D33b", "note": "5a acts on a firing whose check is still paused", "from": "if (firing === null || watchdog.status !== \"up\" || watchdog.lastPingUtcMs === null || watchdog.lastPingUtcMs <= enabledAt) {", "to": "if (firing === null) {" },
  { "id": "D34", "note": "5b down-set not compared as a set (ACT-50)", "from": "if (checks.liveness.status === \"down\" || checks.readiness.status === \"down\") {", "to": "if (false) {" },
  { "id": "D35", "note": "5b counts a down flip from before the disable", "from": "if (flip === undefined || flip.up || flip.utcMs <= disabledAt) {", "to": "if (flip === undefined || flip.up) {" },
  { "id": "D36", "note": "5d accepts a ping from before the re-enable", "from": "lastPing <= reenabledAt", "to": "lastPing < 0" },
  { "id": "D37", "note": "6a disables while a wrapper runs", "from": "if (tasks.cycle.state === \"Running\" || tasks.watchdog.state === \"Running\")", "to": "if (false)" },
  { "id": "D38", "note": "in-flight tolerance of an hour hides the outage signature", "from": "const IN_FLIGHT_TOLERANCE_MS = 120_000;", "to": "const IN_FLIGHT_TOLERANCE_MS = 3_600_000;" },
  { "id": "D38b", "note": "no in-flight tolerance: a finishing invocation invalidates the drill", "from": "const IN_FLIGHT_TOLERANCE_MS = 120_000;", "to": "const IN_FLIGHT_TOLERANCE_MS = 0;" },
  { "id": "D39", "note": "6b local discriminator dropped (ACT-45)", "from": "if (intruders.length > 0) {", "to": "if (false) {" },
  { "id": "D40", "note": "6b counts a down flip from before the disable", "from": "if (flip === undefined || flip.up || flip.utcMs <= silencedAt) {", "to": "if (flip === undefined || flip.up) {" },
  { "id": "D41", "note": "6b counts the drill with the independent read failed", "from": "return drillInvalid(fold, \"6b-silence-down\", \"the independent API read failed\"", "to": "return record(\"6b-silence-down\", \"ok\", {}); drillInvalid(fold, \"6b-silence-down\", \"the independent API read failed\"" },
  { "id": "D42", "note": "6b records on the first check down", "from": "if (Object.keys(downFlips).length < checkNames().length)", "to": "if (Object.keys(downFlips).length < 1)" },
  { "id": "D43", "note": "reboot into checks that are not up", "from": "if (checkNames().some(name => current[name] !== \"up\")) {", "to": "if (false) {" },
  { "id": "D44", "note": "re-arm ignores changed host preconditions", "from": "if (changed.length > 0) {", "to": "if (false) {" },
  { "id": "D45", "note": "explorer process ignored by the signed-out proof", "from": "(sample.interactiveSessions > 0 || sample.explorerProcesses > 0)", "to": "(sample.interactiveSessions > 0)" },
  { "id": "D46", "note": "catch-up firing satisfies the 14:00 proof (ACT-36)", "from": "schedule.anchorDay, 14 * 60, 14 * 60 + 4, [\"run\", \"skip\"]", "to": "schedule.anchorDay, 14 * 60, 14 * 60 + 30, [\"run\", \"skip\"]" },
  { "id": "D47", "note": "session samples from before the boot count", "from": "sample.utcMs > bootUtcMs && ", "to": "" },
  { "id": "D48", "note": "missing 13:5x sample tolerated", "from": "if (before === null) {", "to": "if (false) {" },
  { "id": "D49", "note": "gate accepts a check that is not up", "from": "for (const name of checkNames()) if (current[name] !== \"up\") red.push(", "to": "for (const name of checkNames()) if (false) red.push(" },
  { "id": "D49b", "note": "gate trusts the verdict line over the failed count", "from": "else if (!verifier.value.passed || verifier.value.failedChecks !== 0)", "to": "else if (!verifier.value.passed)" },
  { "id": "D49c", "note": "gate treats unknown as green (ACT-48)", "from": "if (unknown.length > 0 || red.length > 0) {\n    return abortAt(fold, \"10-gate\"", "to": "if (red.length > 0) {\n    return abortAt(fold, \"10-gate\"" },
  { "id": "D50", "note": "gate accepts a boot not later than today's intent (round 5 A2)", "from": "bootUtcMs === null || bootUtcMs <= reboot.intentAtUtcMs) {", "to": "bootUtcMs === null) {" },
  { "id": "D51", "note": "gate leaves the disarm registered", "from": "{ kind: \"delete-disarm\" },", "to": "" },
  { "id": "D52", "note": "anchor window widened to a catch-up", "from": "15 * 60 + 15, 15 * 60 + 19, [\"run\"]", "to": "15 * 60 + 15, 16 * 60, [\"run\"]" },
  { "id": "D53", "note": "a skip at 15:15 counts as the anchor", "from": "15 * 60 + 19, [\"run\"])", "to": "15 * 60 + 19, [\"run\", \"skip\"])" },
  { "id": "D54", "note": "anchor recorded without the BOOTSTRAP entry", "from": "if (firing === null || bootstrap === null)", "to": "if (firing === null)" },
  { "id": "D55", "note": "one unreadable API sample ends the watchdog drill (ACT-47)", "from": "if (!observations.checks.known) return checksUnreadableWait(\"5b-watchdog-down\", schedule, observations.checks.reason);", "to": "if (!observations.checks.known) return abortAt(fold, \"5b-watchdog-down\", \"CHECKS_UNKNOWN\", \"x\");" },
  { "id": "D56", "note": "Running read as unrecognised", "from": "if (state === \"Ready\" || state === \"Running\" || state === \"Queued\") return true;", "to": "if (state === \"Ready\") return true;" },
  { "id": "D57", "note": "tokenizer ignores quotes", "from": "quoted = !quoted;", "to": "quoted = false;" },
  { "id": "D58", "note": "schedule for another anchor day accepted", "from": "if (attempt.anchorDay !== schedule.anchorDay) {", "to": "if (false) {" },
  { "id": "D59", "note": "integrity abort after the gate does not tear down", "from": "reason, teardown: true, nextOwnerAction: \"The ledger cannot be trusted", "to": "reason, teardown: false, nextOwnerAction: \"The ledger cannot be trusted" },
  { "id": "D60", "note": "step 1 idempotence trusts definitions without the verifier", "from": "observations.schedulerCheck.value.passed && observations.schedulerCheck.value.failedChecks === 0", "to": "true" },
  { "id": "D61", "note": "step 4 refuses paused checks", "from": "const notReady = checkNames().filter(name => current[name] !== \"up\" && current[name] !== \"paused\");", "to": "const notReady = checkNames().filter(name => current[name] !== \"up\");" },
  { "id": "D61b", "note": "step 4 enables with a check down", "from": "const notReady = checkNames().filter(name => current[name] !== \"up\" && current[name] !== \"paused\");", "to": "const notReady: string[] = [];" },
  { "id": "D62", "note": "disarm tolerated after the gate", "from": "if (disarm.registered) red.push(\"disarm.expected-deleted-after-gate\");", "to": "" },
  { "id": "D63", "note": "free disk not checked", "from": "observations.freeDiskBytes.value < schedule.minFreeDiskBytes", "to": "false" },
  { "id": "D64", "note": "analyst child start not required", "from": "if (!observations.analyst.value.childStartVerified)", "to": "if (false)" },
  { "id": "D65", "note": "6a accepts a ping from before the watchdog drill ended", "from": "check.lastPingUtcMs <= upAt) allPinged = false;", "to": "check.lastPingUtcMs < 0) allPinged = false;" },
  { "id": "D66", "note": "a host precondition nobody expected passes", "from": "if (!Object.hasOwn(expected, name)) changed.push(name);", "to": "" },
  { "id": "D67", "note": "-MaxLogBytes accepted on the watchdog task, which does not declare it", "from": "        if (name !== \"cycle\") findings.push(`${name}.parameter.unknown:${parameter}`);", "to": "" },
  { "id": "D68", "note": "a key set outside .env ignored", "from": "    if (env.shadowedKeys.length > 0) red.push(", "to": "    if (false) red.push(" },
  { "id": "D69", "note": "step 0 accepts a token that is present but not live", "from": "    if (!observations.analyst.value.tokenLive) red.push(\"analyst.token-not-live\");\n", "to": "\n" },
  { "id": "D70", "note": "the gate accepts a token that died since step 0", "from": "  else if (!observations.analyst.value.tokenLive) red.push(\"analyst.token-not-live\");", "to": "" },
  { "id": "D71", "note": "the gate treats an analyst probe that could not run as green", "from": "  if (!observations.analyst.known) unknown.push(`analyst: ${observations.analyst.reason}`);", "to": "  if (!observations.analyst.known) red.length;" },
  { "id": "D72", "note": "the confirmation cross-check's reasons ignored at step 0 (review 2026-09-14, point 3)", "from": "for (const reason of cross.reasons) red.push(`alert-confirmation.${reason}`);", "to": "" },
  { "id": "D73", "note": "a recorded down flip the live history does not show accepted", "from": "if (cross.downFlipUtcMs[name] !== confirmation.downFlipUtcMs[name]) red.push(", "to": "if (false) red.push(" },
  { "id": "D74", "note": "confirmation dated by the reminder instead of the oldest receipt", "from": "const age = observations.nowUtcMs - cross.oldestReceiptUtcMs;", "to": "const age = observations.nowUtcMs - confirmation.reminderReceivedUtcMs;" },
  { "id": "D75", "note": "the disarm one-shot judged by its time only (review 2026-09-14, point 2)", "from": "        red.push(...disarmFindings(disarm, schedule));", "to": "" },
  { "id": "D76", "note": "a disabled disarm one-shot accepted", "from": "if (disarm.state === null || taskEnabled(disarm.state) !== true) findings.push(", "to": "if (false) findings.push(" },
  { "id": "D77", "note": "a disarm with a second action accepted", "from": "if (disarm.actions.length !== 1 || action === undefined) return", "to": "if (action === undefined) return" },
  { "id": "D78", "note": "the disarm's arguments compared by count only", "from": "tokens.some((token, index) => token.toLowerCase() !== (expected[index] ?? \"\").toLowerCase())) findings.push(\"disarm.arguments\");", "to": "false) findings.push(\"disarm.arguments\");" },
  { "id": "D79", "note": "the disarm's executable not compared", "from": "if (action.execute.trim().toLowerCase() !== schedule.nodePath.toLowerCase()) findings.push(", "to": "if (false) findings.push(" },
  { "id": "D80", "note": "only the cycle wrapper hashed (review 2026-09-14, point 6)", "from": "return [\"cycle-run.ps1\", \"watchdog-run.ps1\"];", "to": "return [\"cycle-run.ps1\"];" },
  { "id": "D81", "note": "the certificate's violations dropped from the abort evidence", "from": "verdict: certificate.verdict, violations: certificate.violations });", "to": "verdict: certificate.verdict });" },
  { "id": "W1", "note": "wiring: 0-resume not called", "from": "const world = worldFindings(fold, observations, schedule);", "to": "const world = { red: [], unknown: [] };" },
  { "id": "W2", "note": "wiring: interrupted reboot not closed", "from": "if (fold.interrupted !== null) return closeInterrupted(fold, fold.interrupted, observations, schedule);", "to": "" },
  { "id": "W3", "note": "wiring: re-arm skips its own checks", "from": "case \"7-rearm\": return step7(fold, observations);", "to": "case \"7-rearm\": return act(\"7-rearm\", [{ kind: \"enable-tasks\", tasks: [\"cycle\", \"watchdog\"] }], {});" },
  { "id": "W4", "note": "wiring: proof recorded without evaluation", "from": "case \"9-proof\": return step9(fold, observations, schedule);", "to": "case \"9-proof\": return record(\"9-proof\", \"ok\", {});" },
  { "id": "W5", "note": "wiring: 5c enables both tasks", "from": "case \"5c-watchdog-reenable\": return act(\"5c-watchdog-reenable\", [{ kind: \"enable-tasks\", tasks: [\"watchdog\"] }], {});", "to": "case \"5c-watchdog-reenable\": return act(\"5c-watchdog-reenable\", [{ kind: \"enable-tasks\", tasks: [\"cycle\", \"watchdog\"] }], {});" }
]
