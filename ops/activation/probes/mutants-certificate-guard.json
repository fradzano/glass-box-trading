[
  {
    "id": "CG1",
    "note": "the coupling applies whatever profile .env names",
    "from": "if (dotEnvProfile === \"competition\" && dotEnvStateDirKey !== null",
    "to": "if (true && dotEnvStateDirKey !== null"
  },
  {
    "id": "CG2",
    "note": "two unusable STATE_DIRs (both null) count as the same directory",
    "from": "&& dotEnvStateDirKey !== null && effectiveStateDirKey",
    "to": "&& true && effectiveStateDirKey"
  },
  {
    "id": "CG3",
    "note": "any effective STATE_DIR is treated as the competition directory",
    "from": "&& effectiveStateDirKey === dotEnvStateDirKey) {",
    "to": "&& true) {"
  },
  {
    "id": "CG4",
    "note": "the coupling refusal is reported as an admission",
    "from": "return { ok: false, reason: \"STATE_DIR resolves",
    "to": "return { ok: true, reason: \"STATE_DIR resolves"
  },
  {
    "id": "CG5",
    "note": "the coupling is never evaluated",
    "from": "if (dotEnvProfile === \"competition\" && dotEnvStateDirKey !== null && effectiveStateDirKey === dotEnvStateDirKey) {",
    "to": "if (false) {"
  }
]
