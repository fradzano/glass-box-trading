[
  { "id": "P1", "note": "an unzoned instant is read as UTC", "from": "(Z|[+-]\\d{2}:\\d{2})$/", "to": "(Z|[+-]\\d{2}:\\d{2})?$/" },
  { "id": "P2", "note": "impossible dates roll over instead of being refused", "from": "if (year < 1970 || roundTrip.getUTCFullYear() !== year || roundTrip.getUTCMonth() !== month - 1 || roundTrip.getUTCDate() !== day", "to": "if (year < 1970 || false" },
  { "id": "P3", "note": "fraction truncated to two digits", "from": "Number(fraction.padEnd(3, \"0\").slice(0, 3))", "to": "Number(fraction.padEnd(3, \"0\").slice(0, 2))" },
  { "id": "P4", "note": "negative offsets read as positive", "from": "const sign = zone.startsWith(\"-\") ? -1 : 1;", "to": "const sign = 1;" },
  { "id": "P5", "note": "byte-order mark kept: the first log line loses its instant", "from": "return text.startsWith(\"\\uFEFF\") ? text.slice(1) : text;", "to": "return text;" },
  { "id": "P6", "note": "skip lines read as other", "from": "const shape = message.startsWith(\"run:\") ? \"run\" : message.startsWith(\"skip:\") ? \"skip\" : \"other\";", "to": "const shape = message.startsWith(\"run:\") ? \"run\" : \"other\";" },
  { "id": "P7", "note": "a line without an instant is skipped instead of making the log unknown", "from": "if (utcMs === null) return unknown(`${file.name}:", "to": "if (utcMs === null) continue; unknown(`${file.name}:" },
  { "id": "P8", "note": "rotation file and current file not merged in time order", "from": "return known([...lines].sort((left, right) => left.utcMs - right.utcMs));", "to": "return known(lines);" },
  { "id": "P9", "note": "a second action on a task accepted", "from": "if (task.value.actions.length !== 1 || action === undefined)", "to": "if (action === undefined)" },
  { "id": "P10", "note": "a task registered twice accepted", "from": "if (found.length > 1) return unknown(", "to": "if (false) return unknown(" },
  { "id": "P11", "note": "a disarm with two triggers accepted", "from": "if (task.value.startBoundaries.length !== 1 || boundary === undefined || boundary === null)", "to": "if (boundary === undefined || boundary === null)" },
  { "id": "P12", "note": "a pass with a non-zero exit code accepted", "from": "if (exitCode !== 0) return unknown(`verifier passed", "to": "if (false) return unknown(`verifier passed" },
  { "id": "P13", "note": "a failure with exit code 0 accepted", "from": "if (verdict === undefined || exitCode === 0) return unknown(", "to": "if (verdict === undefined) return unknown(" },
  { "id": "P14", "note": "two verdict lines accepted", "from": "if (passed.length + failed.length !== 1) return unknown(", "to": "if (passed.length + failed.length === 0) return unknown(" },
  { "id": "P15", "note": "an impossible failure count accepted", "from": "if (verdict.failedChecks < 1 || verdict.failedChecks > verdict.checkCount)", "to": "if (false)" },
  { "id": "P16", "note": "sessions counted instead of distinct accounts", "from": "accounts.add(account.toLowerCase());", "to": "accounts.add(`${account}${String(accounts.size)}`);" },
  { "id": "P17", "note": "window manager and font driver identities counted as people", "from": "if (!/^(DWM|UMFD)-\\d+$/i.test(name)) accounts.add(", "to": "if (true) accounts.add(" },
  { "id": "P18", "note": "every logon type counted", "from": "if (session[\"type\"] !== 2 && session[\"type\"] !== 10 && session[\"type\"] !== 11) continue;", "to": "" },
  { "id": "P19", "note": "the first duplicate wins instead of the last", "from": "    values[key] = value;", "to": "    if (!Object.hasOwn(values, key)) values[key] = value;" },
  { "id": "P20", "note": "quotes not stripped", "from": "if ((value.startsWith(\"\\\"\") && value.endsWith(\"\\\"\")) || (value.startsWith(\"'\") && value.endsWith(\"'\"))) value = value.slice(1, -1);\n    if (seen.has(key))", "to": "if (seen.has(key))" },
  { "id": "P21", "note": ".env beats the environment, the reverse of the runtime", "from": "input.userEnvironment[key] ?? input.machineEnvironment[key] ?? values[key] ?? null", "to": "values[key] ?? input.userEnvironment[key] ?? input.machineEnvironment[key] ?? null" },
  { "id": "P22", "note": "machine environment beats user environment", "from": "input.userEnvironment[key] ?? input.machineEnvironment[key] ?? values[key] ?? null", "to": "input.machineEnvironment[key] ?? input.userEnvironment[key] ?? values[key] ?? null" },
  { "id": "P23", "note": "a key shadowed only by the machine environment not named", "from": "Object.hasOwn(input.userEnvironment, key) || Object.hasOwn(input.machineEnvironment, key)", "to": "Object.hasOwn(input.userEnvironment, key)" },
  { "id": "P24", "note": "an empty certificate line read as absent", "from": "values[key] ?? null", "to": "(values[key] || null)" },
  { "id": "P25", "note": "a certificate of another role accepted", "from": "if (certificate[\"role\"] !== \"dev\") return unknown(", "to": "if (false) return unknown(" },
  { "id": "P26", "note": "a preflight of another profile accepted", "from": "if (report[\"profile\"] !== \"dev\") return unknown(", "to": "if (false) return unknown(" },
  { "id": "P27", "note": "an empty MCP inventory accepted", "from": "mcpTools < 1)", "to": "mcpTools < 0)" },
  { "id": "P28", "note": "a malformed latest confirmation falls back to an older one", "from": "const latest = rows.at(-1);", "to": "const latest = rows.filter(row => parseJson(row).ok).at(-1);" },
  { "id": "P29", "note": "a failed cross-check accepted", "from": "if (line[\"crossCheck\"] !== \"passed\") return unknown(", "to": "if (false) return unknown(" },
  { "id": "P30", "note": "a receipt or down flip missing for one check read as zero", "from": "return liveness === null || readiness === null || watchdog === null ? null : { liveness, readiness, watchdog };", "to": "return { liveness: liveness ?? 0, readiness: readiness ?? 0, watchdog: watchdog ?? 0 };" },
  { "id": "P31", "note": "a reminder list naming something that is not a check accepted", "from": "if (item !== \"liveness\" && item !== \"readiness\" && item !== \"watchdog\") return unknown(", "to": "if (false) return unknown(" },
  { "id": "P32", "note": "any fingerprint text accepted, a UUID included", "from": "/^hc:[0-9a-f]{8}$/.test(value)", "to": "value.length > 0" },
  { "id": "P33", "note": "local time taken in UTC", "from": "timeZone: \"Europe/Berlin\"", "to": "timeZone: \"UTC\"" },
  { "id": "P34", "note": "midnight written as hour 24", "from": "hourCycle: \"h23\"", "to": "hourCycle: \"h24\"" },
  { "id": "P35", "note": "an empty task list is not JSON and becomes unknown", "from": "if (text.trim().length === 0) return known([]);", "to": "" },
  { "id": "P36", "note": "the runtime's validator bypassed: a flat PASS is a PASS (review 2026-09-14, point 1)", "from": "const validation = validator.validate(certificate, validator.expectations);", "to": "const validation = { ok: true as const, successfulDevLiveTestAt: \"\" };" },
  { "id": "P37", "note": "a rejected PASS document read as PASS", "from": "verdict: verdict === \"FAIL\" ? \"FAIL\" : \"REJECTED\"", "to": "verdict: verdict === \"FAIL\" ? \"FAIL\" : \"PASS\"" },
  { "id": "P38", "note": "a confirmation that does not say whether the alert was one mail accepted", "from": "if (typeof bundledAlert !== \"boolean\") return unknown(", "to": "if (false) return unknown(" },
  { "id": "P39", "note": "the disarm's second action dropped before the core can see it (review 2026-09-14, point 2)", "from": "state: task.value.state, actions: task.value.actions, runLevel", "to": "state: task.value.state, actions: task.value.actions.slice(0, 1), runLevel" },
  { "id": "P40", "note": "the disarm's principal and settings reported as the spec wants them, whatever was registered (review 2026-09-14, point 4)", "from": "runLevel: task.value.runLevel, logonType: task.value.logonType, startWhenAvailable: task.value.startWhenAvailable, userId: task.value.userId, userSid: task.value.userSid });", "to": "runLevel: \"Highest\", logonType: \"S4U\", startWhenAvailable: true, userId: task.value.userId, userSid: task.value.userSid });" },
  { "id": "P41", "note": "a numeric RunLevel accepted as if the reader's shape had not changed", "from": "if (runLevel !== null && typeof runLevel !== \"string\") return unknown(", "to": "if (false) return unknown(" },
  { "id": "P42", "note": "a StartWhenAvailable that is not a boolean accepted", "from": "if (startWhenAvailable !== null && typeof startWhenAvailable !== \"boolean\") return unknown(", "to": "if (false) return unknown(" },
  { "id": "P43", "note": "the logon type never read", "from": "const logonType = item[\"LogonType\"] ?? null;", "to": "const logonType = null;" },
  { "id": "P44", "note": "any line that mentions an armed book recovery read as the armed composition", "from": "if (/^output: watchdog composed for the \\S+ profile over .+; book recovery armed$/.test(message)) return \"armed\";", "to": "if (message.includes(\"book recovery armed\")) return \"armed\";" },
  { "id": "P45", "note": "the degraded composition not recognised (spec §8.12)", "from": "  if (message.startsWith(\"output: watchdog book recovery unavailable, fencing and halting only: \")) return \"degraded\";", "to": "" }
]
