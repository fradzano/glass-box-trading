# Cold read of CONCEPT.md — 2026-08-24

One unprimed adversarial reader (fresh agent, no builder context) attacked the
concept against the hackathon requirements. Published here on purpose — glass box
includes the design's failure modes. Severity: A = would break an invariant, the
submission, or the schedule; B = material gap with workaround; C = polish.

| # | Sev | Finding (condensed) | Disposition |
|---|---|---|---|
| 1 | A | Gate catalog was entry-only: no exit/expiry/assignment gates. Weekly SPY spreads + early assignment turn a defined-risk spread into an unclassified stock+leg position no gate ever saw. | Fixed: lifecycle gates added (expiry eviction, assignment reconciliation) — CONCEPT §4 |
| 2 | A | Crash between order submit and journal write: budget never debited, position exists without a journaled decision. Idempotency IDs don't help (next cycle proposes different candidates). | Fixed: intent entry journaled BEFORE submit + start-of-cycle reconciliation against Alpaca — §3/§4 |
| 3 | A | Budget debited at fill, not at submit: two open limit orders can double-spend a sleeve with all gates green. | Fixed: reservation at submit; open orders part of the snapshot — §4 |
| 4 | A | No cycle mutex, no persisted halt flag: kill-switch/watchdog and an in-flight cycle can cross; new risk after "flatten + halt". Also overlapping orchestrator instances. | Fixed: single-instance lock, halt file as core input, snapshot staleness bound — §3/§4 |
| 5 | A | Schedule had zero contingency and put the only untested mechanic (credit-order acceptance, market-hours-only testable) after go-live. | Fixed: Monday pre-arm live test on dev account; Tuesday declared buffer — §8 |
| 6 | A | Dead-man watchdog as stated: nightly false-stale (session gate stops heartbeats), leg-wise flatten creates transient naked short, and host death kills agent+watchdog together. | Fixed: market-hours-aware watchdog, whole-structure (mleg) closes only; host death accepted and declared — defined-risk cap is the real backstop — §4 |
| 7 | B | Per-cycle journal pushes collide with branch convention and human work in the same repo. | Fixed: agent owns a dedicated `journal` branch (Vercel prod branch); humans never commit there — §5 |
| 8 | B | "Income ~12 %" had no unit — credit structures RECEIVE premium. | Fixed: income budget measured in sum of max loss (width − credit) — §2 |
| 9 | B | Unattended-host hardening absent (sleep, reboots, run-when-logged-off). | Fixed: hardening checklist added to schedule (Fri) — §8 |
| 10 | B | Analyst failure path unspecified (rate limits shared with the owner's own plan usage). | Fixed: analyst error ⇒ skip cycle + journal entry, never crash-loop — §3 |
| 11 | B | Kill-switch threshold could be tripped by the convex sleeve's PLANNED decay; no un-halt semantics. | Fixed: threshold must price in planned convex loss; un-halt is a manual, journaled owner action — §4 |
| 12 | B | Cover image, descriptions, tags, deck tooling unscheduled; Wednesday overloaded. | Fixed: submission checklist spread across §8 |
| 13 | C | Positions alive past the deadline. | Adopted: flatten everything by Wednesday close, journaled — §8 |
| 14 | C | Social row in judging map depends on undecided O4. | Marked conditional — §7 |
| 15 | C | "TypeScript end-to-end" vs. Python MCP server. | Wording fixed — §5 |

Verified solid by the reader: requirement fit (MCP+CLI, options, fresh account,
MIT, Vercel), the declared-variance framing, no US market holiday in the window,
Vercel deploy limits sufficient, account separation mechanics.

Process note: this was a single-round cold read, not a full bis-0 loop — the
full discipline (tests + cold read at 0) is reserved for the gate core CODE
during the build weekend.
