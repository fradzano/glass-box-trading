# P12 — resume after the Monday review

Prepared 2026-09-21 CEST for a fresh Claude session. This is a review handoff, not
an owner ruling, a counted gate, a new verification run or permission to activate.
The reviewed code is `c762d2c`, branch `p7/dev-live-certificate`; this handoff changes
documentation only.

## Read first and preserve continuity

Read `AGENTS.md` / `CLAUDE.md` and their imports, `REPO_MAP.md`, `TEST_MAP.md`,
`STATE.md`, `DECISIONS.md`, then the existing run's books and primary returns.
The run store is
`C:\Users\felix\verify-runs\fradzano\glass-box-trading\p12-units-1-11`.
At review it was DISARMED, round 4 of 8, archiver `claude`, last lease
`949fe3017dcf46c887a6d728d7d21b29`. Resume that run using the supported Claude
archiver procedure and current lease rules. Do not create a new run, rebind it to
Codex, manually edit its binding, or reuse a disarmed lease as if it were active.
The old session is being retired for context reasons, not because the run ended.

The implementation entry point is [P12-ACTIVATION-BUILD.md](P12-ACTIVATION-BUILD.md),
read with the current [activation spec](P12-ACTIVATION-SPEC.md),
[stop/log contracts](P12-STOP-AND-LOG-CONTRACTS.md) and
[runbook](P12-RUNBOOK.md). Historical status paragraphs can be stale; the current
code and the primary gate returns must settle each discrepancy.

## What the independent review measured

- Full `npm.cmd run verify`: exit 0, 709 root tests and 726 passed / two skipped
  activation tests, 728 collected. The two skips are weekday-dependent cases.
  Two installer cases failed inside the Codex sandbox; the 58-case process file
  and then the full check passed outside it with a non-administrator token.
- Both real deployment tasks remain disabled with old definitions. The live
  verifier fails 4/58 checks. `GlassBoxTrading-Activation` is not registered.
- The live bootstrap directory and file have protected, narrow ACLs and the
  directory contains exactly `healthchecks-watchdog.url`. No endpoint was printed.
- No real installation, activation, certificate run or broker action was made.
  These measurements do not change any formal finding status.

## Remaining work, in dependency order

1. **Separate missing evidence from missing implementation.** The seven open A
   rows are R1-14, R1-21, R2-29, R3-08, R3-09, R4-01 and R4-02. Match each to the
   current implementation and its primary returns, then obtain the required
   counted counterverification. Do not infer closure from green tests. In
   particular, the stop rebuild for R3-08/R3-09 was already authorised and built
   on 2026-09-19; do not ask for that same permission again. R4-01/R4-02 lack
   archived closing returns. The calendar cause still exists in
   `src/shell/watchdog-cli.ts` (`isTradingDay: true`); the wrapper table is a
   mitigation, not an accepted closure of its refused residual.
2. **Finish the PowerShell/security boundary.** Installer/verifier process
   coverage is formally RESOLVED; bootstrap write coverage remains PATCHED.
   Review the existing disposable elevated bootstrap proof before proposing its
   execution. The remaining SetOwner call, four callsites, needs real evidence;
   three rejected residual drafts are not acceptance. Pre-created foreign files
   and orphan `.tmp-*` / `.bak-*` files need the disposition required by the run.
   Existing production-edit/seam restrictions still apply: propose the coherent
   change and obtain any required new authority rather than silently patching.
   The current live secret has narrow ACLs; the precreation finding concerns the
   installation boundary. Test callsite replacement is not OS confinement.
   Do not run the adversarial suite elevated on the real host on that assumption.
3. **Implement and verify Unit 13.** Bind the actual action ports, register the
   activation task through the intended installer, wire and prove the real page,
   and rehearse the end-to-end paths. `ops/activation/cli.ts` still has
   `actions: null` and a stderr-only page. The alert-channel choice is already
   decided (build log, 2026-09-17); do not request that decision again. Keep real
   host writes behind their specific authority and prerequisites.
4. **Complete the operating proofs and activation sequence.** Use the runbook's
   order: supervised dev certificate when admitted, elevated replacement of the
   two task definitions with both left disabled and the verifier fully green,
   then the admitted drills and cold-start sequence. Literal signed-out S4U,
   reboot without login, restart/one-writer and failure/recovery evidence remain
   necessary. Session 0 alone does not prove a signed-out desktop. Coordinate
   actual logout/reboot with Felix. A missing proof moves the anchor.

## Certificate boundary: check before promising reuse

`src/shell/digests.ts:enumerateRuntimeFiles` is authoritative. The runtime digest
includes `src/**/*.ts`, `dist/**/*.js`, everything under `assets/`,
`config/**/*.json`, `tools/**/*.mjs`, `tools/**/*.py`, `package.json`,
`package-lock.json`, `tsconfig.json` and `tsconfig.build.json`, plus the verified
analyst-runtime identity. The policy digest must match too.

Pure changes under `ops/activation`, PowerShell `.ps1` / `.psm1` changes, docs and
formal evidence bookkeeping do not by themselves change those digests. A Unit 13
npm script in root `package.json`, a new dependency, changed configuration or a
calendar repair under `src/` does. Do not narrow the digest or move production
logic outside it to preserve a certificate. Hash equality is necessary for reuse;
it does not waive any operating or activation evidence.

A certificate now could be useful as a supervised dev integration measurement,
but reuse is not guaranteed while the calendar finding and Unit 13 implementation
surface remain unsettled. The current runbook requires Unit 13 and operating
proofs before the certificate; advancing it needs an explicit owner decision.
No such decision or run is recorded by this handoff. First enumerate the planned
digest-affecting edits; complete them before a final certificate if possible.

The workload is several remaining engineering/proof packages, not an install and
one click. Unit 13 is the largest known implementation item; formal gates can
still generate work. No measured basis supports an hours-to-completion promise.
The operating sequence also spans an evening and the following morning.
